import React from "react"

export default ({children}) => (
    <div className="container mx-auto">
        <div className={"mx-5 mt-8 md:mx-10 md:mt-16"}>
            {children}
        </div>
    </div>
)
